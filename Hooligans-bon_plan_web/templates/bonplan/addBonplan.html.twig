{% extends "index.html.twig" %}
{% block body %}

<div class="box-container">
{# <div style="margin : 3em;" class="login-box">
  <h2>Add bonplan</h2>
  <form>
  {{ form_start(f,{'attr': {'novalidate': 'novalidate','style': 'display: flex;'  }} ) }}
    <div class="user-box">
      
      {{ form_widget(f.nom_bonplan) }}
      {{ form_errors(f.nom_bonplan) }}
      <label>{{ form_label(f.nom_bonplan) }}</label>
    </div>
    <div class="user-box">
      {{ form_widget(f.gouvernorat) }}
      <label>{{ form_label(f.gouvernorat) }}</label>
    </div>
    <div class="user-box">
      
      {{ form_widget(f.ville) }}
      <label>{{ form_label(f.ville) }}</label>
    </div>
    <div class="user-box">
      
      {{ form_widget(f.rue) }}
      <label>{{ form_label(f.rue) }}</label>
    </div>
    <div class="user-box">
      {{ form_widget(f.description) }}
      <label>{{ form_label(f.description) }}</label>
    </div>
    <div class="user-box">
      
      {{ form_widget(f.type) }}
      <label>{{ form_label(f.type) }}</label>
    </div>
    <div class="user-box">
      
      {{ form_widget(f.etat) }}
      <label>{{ form_label(f.etat) }}</label>
    </div>
    <div class="user-box">
     
      {{ form_widget(f.imageFile, {'attr': {'onchange': 'previewImage(this)'}}) }}
      
      <img id="image-preview" src="#" alt="Selected Image" style="display: none; max-width: 200px;">
    </div>
      <a href="#">
      <span></span>
      <span></span>
      <span></span>
      <span></span>

      {{ form_widget(f.save) }}
      
    </a>

  {{ form_end(f) }}
  </form>
</div> #}

<div>
</div>

</div>




    

   

    

    <script>
    function previewImage(input) {
        if (input.files && input.files[0]) {
            var reader = new FileReader();

            reader.onload = function (e) {
            var img = document.getElementById('image-preview');
            img.src = e.target.result;
            img.style.display = 'block';
        };

        reader.readAsDataURL(input.files[0]);
        }
        }
    </script>





{% endblock %}
